{
  "version": 2,
  "buildCommand": "npx prisma generate && npx prisma db push --accept-data-loss",
  "functions": {
    "api/index.js": {
      "maxDuration": 30
    }
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/api/index.js"
    }
  ]
}
